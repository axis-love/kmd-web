// @axis-love/styles
// Scoped reader CSS and generated design tokens.
// CSS files have side effects (they are imported for their styles).

/**
 * The version of the token schema these styles were generated from.
 * Matches the `version` field in tokens/tokens.json.
 */
export const TOKENS_VERSION = "1.0.0";

/**
 * The @axis-love/styles package version.
 */
export const STYLES_VERSION = "0.1.0-rc.1";

/**
 * The list of theme variants defined in the canonical token schema.
 * Consumers can use this to validate theme identifiers before applying them.
 */
export const TOKEN_THEMES: readonly string[] = ["dark", "light"] as const;

/**
 * The default theme identifier from the canonical token schema.
 */
export const DEFAULT_THEME = "dark" as const;
