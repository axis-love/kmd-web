# Footnotes

A sentence with a footnote reference.[^1]

Another sentence with a different footnote.[^2]

A footnote with multiple references.[^1]

[^1]: First footnote with **bold** and `code`.

[^2]: Second footnote.

    Multi-paragraph footnote content
    is supported with indentation.

A footnote referenced but never defined.[^3]