# tooling

Shared build and release tooling for the kmd-web workspace.

This directory will contain:
- Token generation scripts (KWEB-006)
- Bundle size measurement scripts (KWEB-018)
- Package-contents verification scripts (KWEB-016)
- Release automation helpers (KWEB-016)

Currently empty — tooling is added per-task as needed.